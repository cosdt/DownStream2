name: Downstream CI

on:
  repository_dispatch:
    types: [trigger-b-ci]

jobs:
  ci:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run CI
        run: |
          echo "Running B CI..."
          sleep 10
          echo "Done"
